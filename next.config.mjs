/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['jaracake.com', 'glamorganbakery.com', 'i.pinimg.com', 'blogger.googleusercontent.com', 'www.eatlanka.com'], // Add the external domain here
    },
  };
  
  export default nextConfig;
  